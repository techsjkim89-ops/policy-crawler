# 외국인 정책 크롤링 서버 환경변수 (Firestore 전용)

# 서버 포트
PORT=5000

# ========== Firebase Firestore 설정 ==========
# 옵션 1: 서비스 계정 키 파일 경로 (개발 환경)
FIREBASE_SERVICE_ACCOUNT_PATH=./serviceAccountKey.json

# 옵션 2: 서비스 계정 키 JSON 문자열 (배포 환경)
# FIREBASE_SERVICE_ACCOUNT={"type":"service_account","project_id":"your-project",...}

# Firebase Storage 버킷 (선택사항)
# FIREBASE_STORAGE_BUCKET=your-project.appspot.com

# ========== 중요 ==========
# 1. Firebase Console에서 프로젝트 생성
# 2. 프로젝트 설정 > 서비스 계정 > 새 비공개 키 생성
# 3. 다운로드한 JSON 파일을 'serviceAccountKey.json'으로 저장
# 4. 이 파일을 server 폴더에 배치

# .gitignore에 추가 필수:
# serviceAccountKey.json
# .env
